//
//  HistoryViewController.swift
//  simple-calc-iOS
//
//  Created by Tejveer Rai on 10/19/18.
//  Copyright © 2018 Tejveer Rai. All rights reserved.
//

import UIKit

class HistoryViewController: UIViewController {
/*
    @IBOutlet weak var scroll: UIScrollView!
    
    override func viewDidLoad() {
        super.viewDidLoad()
        // Do any additional setup after loading the view.
        print("loaded")
    }
    
    @IBAction func yeet(_ sender: Any) {
        self.dismiss(animated: true, completion: nil)
        print("clicked");
    }
    
    override func viewDidAppear(_ animated: Bool) {
        var currY = 100;
        let framewidth = self.view.frame.width;
        for str in history{
            let label = UILabel();
            label.frame = CGRect(x: 0, y: Double(currY), width: Double(framewidth), height: 120);
            label.textAlignment = .center;
            label.textColor = UIColor.black;
            label.font = UIFont(name: "Menlo", size: 28);
            label.text = str;
            self.view.viewWithTag(10)!.addSubview(label);
            currY += 100;
        }
        print("hello")
    }
    /*
    // MARK: - Navigation

    // In a storyboard-based application, you will often want to do a little preparation before navigation
    override func prepare(for segue: UIStoryboardSegue, sender: Any?) {
        // Get the new view controller using segue.destination.
        // Pass the selected object to the new view controller.
    }
    */
*/
}
